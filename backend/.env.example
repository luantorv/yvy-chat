PORT=3001
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=argano-contract-assistant
# Carpeta con el corpus base (.md, .txt, .pdf) que se indexa al arrancar.
# Por defecto es ./data relativo al cwd del proceso (backend/data).
DATA_DIR=./data
